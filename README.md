# Bokoblin Attack!

<i>Don't let Bokoblin squash you</i>

## To start the server...
`python3 -m http.server 5500`

## GitHub flow...
After you make code updates (after each feature), commit your changes via the following:

`git add .`

`git commit -m "Short summary: what changed (why)`

Then push your local change to the main repository:

`git push`
